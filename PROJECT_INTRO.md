════════════════════════════════════════════════════════════════
  EvoAgent —— 自进化 AI 硬件开发系统
  （智慧养老跌倒报警系统的进化形态 · 2026-08-21 更新版）
════════════════════════════════════════════════════════════════

【一句话介绍】
一个由自然语言（语音为主，文字/API 也可）驱动的自进化系统：
你说"给板子加个功能"→ AI 自动改代码、编译、无线 OTA 升级到板子上；
遇到全新的陌生板，云端 AI 指挥"烧录板"通过串口给它烧入引导固件；
系统缺什么能力就自己造工具装上，越用越贴合你的需求。

────────────────────────────────────────────────────────────────
一、系统全景（当前已实现 + 规划中）
────────────────────────────────────────────────────────────────
┌──────────────────────── 云端（阿里云 YOUR_SERVER_HOST）────────────────────────┐
│ DSH ×2（AI 开发大脑）：DSH-1 干活 / DSH-2 插件进化（隔离环境）              │
│ fall-mcp 能力中枢（:8002）：26+ MCP 工具 + 动态工具库 + 记忆层 + 经验索引   │
│ xiaozhi-server 语音大脑（:8001/:8003）· fall server API（:8000）           │
│ Caddy（:80 静态 OTA）· mosquitto MQTT（:1883 事件/遥测/命令三通道）         │
│ systemd 守护全服务 · /opt/ota/<设备>/ 固件仓库（full.bin + latest.json）    │
└──────────────────────────────────┬──────────────────────────────────────────┘
        WiFi/语音                 WiFi/MQTT 事件               WiFi/MQTT 事件
┌───────────┴──────────┐   ┌───────────┴──────────┐   ┌───────────┴──────────┐
│ 语音板（ESP32S3-N16R8）│   │ 烧录板（独立执行器）  │   │ OLED 显示板（退役）│
│ 用户的嘴和耳朵        │   │ v12 指令执行器        │   │ MQTT 订阅+SSD1306 HUD │
│ （规划：OLED+出错播报）│   │ GPIO1/2 UART 烧录口   │   │ 实时显示烧录进度      │
└───────────────────────┘   └───────────┬──────────┘   └──────────────────────┘
                                         │ UART 串口（首次烧录唯一物理连接）
                                ┌────────┴────────┐
                                │ 目标板（任意 ESP32）│
                                │ 白片→引导固件→OTA  │
                                └───────────────────┘

────────────────────────────────────────────────────────────────
二、硬件现状（2026-08-21 实测）
────────────────────────────────────────────────────────────────
| 设备 | 角色 | 固件 | 状态 |
|---|---|---|---|
| 跌倒板 ESP32S3-Cam | 跌倒检测（雷达+视觉） | v1.6.1 | 业务正常，OTA 已验证 |
| 测试板1（N16R8，MAC xx:xx:xx:xx:xx:xx，COMxx） | **云端烧录板** | **v12（MQTT 指令执行器）** | 指令流验证 8/8 PASS（flash_start/waiting/busy/abort/status/日志回传） |
| 测试板2（N16R8，MAC 末 0f81d8，COMxx） | **语音板（evo-voice-v1）** | xiaozhi v2.4.2 干净重建 | **唤醒「你好小安」**：INMP441(12/13/21)+MAX98357A(4/14/18/5)，供电需充电器 |
| 测试板3 | 弃用 | - | 挑固件怪板，已弃用 |
| 语音板（旧，COMxx） | 已退役 | 旧固件 | 由测试板2 替代 |

────────────────────────────────────────────────────────────────
三、核心能力（全部已验证或已实现）
────────────────────────────────────────────────────────────────
1. 语音驱动开发：说话 → 语音板 → xiaozhi → DSH 理解执行
2. 工具工厂：DSH 用 LLM 生成 MCP 工具（13+ 动态工具，热加载）
3. 固件 OTA 迭代：dev_ota_deploy → DSH 改码 → 编译 → 归档 → 推送 → 无线升级（双分区回滚）
4. 多板管理：devices.json 注册表 + dev_register_board/dev_list_boards + 动态板规格
5. 记忆与经验：字符 bigram 经验索引 + 进化日志 + 工具调用统计
6. 新板零代码接入：boards/ 动态发现 + MQTT ACL 通配 + OTA 路由通配
7. 【新增】云端烧录：烧录板 WiFi 下载 full.bin → 串口流式烧录 → MD5 校验（v2 基线 100% PASS）
8. 【新增】OLED 独立显示：烧录事件经 MQTT（fall/flasher/events）驱动显示板 HUD
9. 【新增】事件协议：ready/waiting/connected/progress/done/error 六类事件
10. 【新增】MQTT 指令执行器（v12）：fall/commands/flasher-board 通道
    flash_start(device|url+size)/abort/status，开机不自动烧录，日志回传 fall/flasher/log

────────────────────────────────────────────────────────────────
四、进度总览
────────────────────────────────────────────────────────────────
✅ 阶段0 基线 · ✅ 阶段1 服务器化 · ✅ 阶段2 固件可靠直连
✅ 阶段3 APP 后端 · ✅ 阶段5 语音板联动 · ✅ 阶段4 OTA
✅ 方向A 多设备泛化（新板接入/注册表/多板 OTA）
✅ 板烧板读/写验证 · ✅ 云端烧录闭环 · ✅ 烧录/显示分离架构
✅ OLED 接线验证（2026-08-21）· ✅ #7 MQTT 指令执行器化（v12，2026-08-21）
⏸️ 跳过：端到端烧录演示（无可用目标板，有板后可补）
📌 后续：#8-#19（监工/语音转述/权限/确认/自检/编排/防呆/配网）
⏸️ 暂缓：#12 MAC 核对 / #13 断点续烧 / #14 错误码体系

────────────────────────────────────────────────────────────────
五、关键踩坑沉淀（经验库）
────────────────────────────────────────────────────────────────
· 烧录后"静默"多为复位时机/下载模式，用 esptool run 复位 + DTR/RTS 拉高监听
· IDLE 任务栈配置名 CONFIG_FREERTOS_IDLE_TASK_STACKSIZE（无下划线），溢出导致烧录崩溃
· OLED/I2C 与烧录高压并发会崩溃 → 显示功能必须独立板（MQTT 事件驱动）
· N16R8 需 CONFIG_SPIRAM=y + OCT；flash 兼容问题用 DIO/40M 保守配置
· 版本比较兼容 v 前缀；PowerShell 引号一律脚本文件；scp 多目标分开传
· cJSON_Delete 后严禁再用其指针（use-after-free）；服务器编译需 IDF_TOOLS_PATH=/opt/esp

════════════════════════════════════════════════════════════════
