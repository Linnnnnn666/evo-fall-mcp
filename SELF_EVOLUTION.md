════════════════════════════════════════════════════════════════
  EvoAgent 自进化系统解析（给新会话的核心概念课 · 2026-08-21）
════════════════════════════════════════════════════════════════

一、一句话定义
────────────────────────────────────────────────────────────────
自进化 = 系统通过"干活→验证→沉淀"的循环，不断长出新的能力、
积累经验、修正错误，下一次遇到同类问题直接调用已有能力，
不再需要从头再来。人负责"说要什么"，系统负责"学会怎么干"。

二、进化循环（五步闭环，核心机制）
────────────────────────────────────────────────────────────────
┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐
│ 1. 需求   │ → │ 2. 行动   │ → │ 3. 验证   │ → │ 4. 沉淀   │ → │ 5. 复用   │
│ 用户说    │   │ DSH 干活  │   │ 结果检查  │   │ 经验入库  │   │ 下次直接  │
│ "查银价"  │   │ 写工具/码 │   │ 实测通过  │   │ 工具注册  │   │ 调用/注入 │
└─────────┘   └─────────┘   └─────────┘   └─────────┘   └─────────┘
                                                    ↓ 回到 1（能力变多）

三、系统的"进化器官"（每个都有真实代码/文件）
────────────────────────────────────────────────────────────────
| 器官 | 机制 | 落盘位置 |
|---|---|---|
| 工具工厂 | LLM 生成 Python 工具模块 → 编译+热加载验证 → 注册进 MCP | dynamic_tools/ + manifest.json |
| 记忆层 | 工具元数据 + 调用统计（谁造的、用过几次、效果） | manifest.json + evolution.log |
| 经验索引 | 任务结果文件 → 字符 bigram 相似度匹配 → 新任务自动注入【参考经验】 | task_results/ + 索引 |
| DSH-2 进化者 | 独立隔离环境造插件（装坏不影响主系统，可回滚） | headless-builder profile + dsh-plugins/ |
| 插件轮询器 | 检测 plugin_requests/ 新需求 → 自动造插件 | fall-mcp 后台线程 |
| 审计日志 | 高危操作全量留痕（谁/何时/结果）→ 进化素材 | audit（规划中） |
| 经验质量标记 | 任务成功/失败反馈到经验 score（方向 B，规划中） | 经验索引（规划中） |

四、真实进化案例（本项目实际发生过的）
────────────────────────────────────────────────────────────────
1. 【长出工具】用户语音"查一下银价" → DSH 生成 query_silver_price.py →
   验证通过 → 注册进 manifest → 之后语音直接调用（调用次数累积在册）
2. 【长出固件能力】"接入一块新板" → 从零建出 board-template 通用引导固件
   （配置化 boards/*.h + OTA + 遥测）→ 之后每块新板复用模板
3. 【长出烧录能力】用户设想"语音板烧新板" → 阶段1/1b/2 逐步验证 →
最终定型为独立烧录板 v11 + OLED 显示板（2026-08-25 OLED 显示板改造为语音板 evo-voice-v1）
4. 【经验修正错误】板子 qio 起不来 → 沉淀"DIO/40M 编译进 bootloader"；
   MQTT 命令收不到 → 沉淀"ACL 通配 fall/commands/#"；版本自刷 bug →
   修复 fw_newer；烧录崩溃 → 沉淀"OLED 与烧录物理分离 + IDLE 栈名"——
   这些坑都已写进工程配置/文档，新板接入不会再踩
5. 【经验复用】任务"上证指数"执行后，后续相似任务（查股票/行情类）自动
   注入历史经验（实测 bigram 匹配 12 分命中）
6. 【自我组织】DSH-2 在隔离环境造出 base64-codec/reverse-string/text-stats
   插件——系统自己给自己造工具（当前最接近"自进化"字面意义的案例）

五、进化与安全的平衡（为什么不是"失控的自进化"）
────────────────────────────────────────────────────────────────
| 设计 | 理由 |
|---|---|
| DSH-2 隔离环境 | 进化实验在沙盒，装坏插件不影响主系统，可回滚 |
| **两层进化保险（2026-08-21 落地）** | **DSH-2 给 DSH-1 装坏插件时：健康检测自动发现 → 确定性修复（注释隔离坏条目）→ 仍坏则 DSH-2（隔离环境，自身不会坏）自动兜底修复**——用户设计的保险机制，实测双场景通过 |
| 修复需人工确认（方向C） | 自主升级的边界：破坏性/影响业务的修复必须人点头 |
| 工具生成后编译+验证才注册 | 不合格工具进不了工具库 |
| 开发者模式门控（规划） | 生产工具不被误触发/幻觉调用 |
| 经验质量标记（规划） | 坏经验会被降权，防止"越进化越歪" |
| 审计留痕 | 进化每一步可回溯，出问题能查"谁教会了系统什么" |

五·五、两层进化保险机制（DSH-1/DSH-2 隔离修复，2026-08-21 实测落地）
────────────────────────────────────────────────────────────────
设计意图（用户定义）：DSH-2 只服务 DSH-1（造插件帮 DSH-1 进化）；若装坏导致
DSH-1 无法工作，DSH-2 必须能修复 DSH-1——前提是两者分离，否则 DSH-2 装坏
自己也跟着坏，无人可救。

实现（dev_tool_factory.py）：
1. `_health_check_headless()`：静态健康检查（秒级）——patch 存在 / YAML 合法 /
   insert 引用文件存在 / 插件 node --check 语法
2. `_auto_repair_headless()`：确定性自动修复——file_missing/syntax_error 按文本块
   注释隔离坏条目 + 文件移入 /opt/dsh-plugins/quarantine/；patch_broken 恢复最近备份
3. `_repair_with_dsh2()`：DSH-2 兜底——确定性修复失败时起隔离环境 DSH-2
   （HEALTH_REPAIR_TEMPLATE）智能诊断修复 + 冒烟验证
4. 触发点：任务派发失败后 / 插件安装完成后 / 轮询器每 30 轮周期检查；
   防并发锁 /tmp/dsh-headless-repair.lock
5. 留痕：evolution.log 记录 health_check / auto_repair_done / auto_repair_fallback

实测（2026-08-21）：
- 场景 A（装坏插件）：注入引用不存在文件的条目 → 检出 file_missing → 确定性修复秒级完成
- 场景 B（patch 彻底损坏无备份）：检出 patch_broken → 确定性修复放弃 → DSH-2 兜底
  98 秒重建 patch（5 条目全保留）+ YAML/引用/冒烟三重验证通过

六、当前进化水平（诚实评估）
────────────────────────────────────────────────────────────────
· 已具备：工具自动生成与注册、经验自动注入、固件能力自建（模板化）、
  踩坑沉淀为工程配置、插件隔离制造
· 已演示：完整闭环（需求→DSH→验证→入库→复用）在工具/固件两条线上跑通
· 待增强：经验质量标记（方向B）、调度器模拟主管（方向C）、
  意图确认与权限门控（#10/#11）、语音板集成（#16）——都在路线图

七、终极形态（自进化到什么程度）
────────────────────────────────────────────────────────────────
"用户说'我想让板子能做 X'——系统自动：判断缺什么能力（工具？固件？硬件？）
→ 自己造/自己写 → 验证 → 装到对应设备 → 告诉用户'好了，以后直接说就行'。
系统每完成一次这样的循环，就多一项能力、多一份经验，
于是同样的需求第二次出现时更快，全新的需求也更容易上手——
这就是'越来越贴合用户需求'的机制。"

════════════════════════════════════════════════════════════════
新会话注意：本项目"自进化"= 具体机制（工具工厂/经验索引/插件制造/
能力模板化/踩坑沉淀），不是抽象口号——所有环节都有代码和文件可查。
════════════════════════════════════════════════════════════════
