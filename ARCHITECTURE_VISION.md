════════════════════════════════════════════════════════════════
  EvoAgent 最终架构构思（完整蓝图 · 2026-08-21）
  角色模型：顾客（用户）· 服务员（语音 LLM）· 厨师（DSH）· 双手（设备）
════════════════════════════════════════════════════════════════

一、愿景
────────────────────────────────────────────────────────────────
任何人（包括不懂硬件的家人）用自然语言就能：
  接入新板 → 开发功能 → 迭代升级 → 排查故障，
全程由 AI 系统自主编排，人只负责"说"和"按一下确认"。
系统自我进化：缺工具自己造、经验自动沉淀、质量可标记。

二、角色模型（用户比喻 → 架构映射）
────────────────────────────────────────────────────────────────
| 比喻 | 组件 | 职责 |
|---|---|---|
| 顾客 | 用户 | 下达意图、听/看结果、物理确认 |
| 服务员 | xiaozhi 对话 LLM | 转述、总结、意图确认、记忆聊天上下文 |
| 厨师 | DSH（headless 任务） | 理解需求→改码→编译→决策→监工→干预 |
| 传菜口 | fall-mcp + MQTT | 指令下行、事件上行、工具注册 |
| 双手 | 烧录板/目标板/语音板 | 执行烧录、运行固件、播报/显示 |

三、系统拓扑（最终形态）
────────────────────────────────────────────────────────────────
┌────────────────────────── 云端 ──────────────────────────┐
│ DSH-1（干活）        DSH-2（插件进化，隔离环境）          │
│ fall-mcp 能力中枢：dev_* 工具集 + 动态工具 + 经验索引      │
│   开发者模式门控（工具列表按模式过滤）                     │
│   烧录任务记录器（事件流水 = 唯一事实源）                  │
│   dev_speak（厨师→服务员播报）/ dev_talk_to_dsh（反馈通道）│
│ xiaozhi-server（服务员 LLM + TTS/ASR）                    │
│ MQTT：fall/flasher/events · fall/commands/<id>            │
│       fall/telemetry/<id> · devices/+/events/fall          │
│ OTA 仓库：/opt/ota/<设备>/full.bin + latest.json           │
└──────────────────────────┬───────────────────────────────┘
        WiFi                  WiFi/MQTT               WiFi/MQTT
┌────────┴────────┐   ┌────────┴────────┐   ┌────────┴────────┐
│ 语音板（小安）    │   │ 烧录板（独立）   │   │ OLED 显示板（退役）│
│ 语音交互         │   │ v2 基线稳定内核  │   │ MQTT 订阅       │
│ OLED 状态显示    │   │ MQTT 事件上报    │   │ SSD1306 HUD     │
│ 出错时 TTS 播报  │   │ GPIO1/2 UART 口  │   │ 进度/状态实时    │
└─────────────────┘   └────────┬────────┘   └─────────────────┘
                               │ 4 根线（TX/RX/3V3/GND）+ BOOT+RST
                     ┌─────────┴─────────┐
                     │ 目标板（任意 ESP32）│
                     │ 白片→引导固件→OTA  │
                     └───────────────────┘

四、核心链路（一次完整的新板接入）
────────────────────────────────────────────────────────────────
1. 用户（语音）："接入一块新板"
2. 服务员：询问型号/MAC → LLM 理解 → **意图级复读确认**（含功能描述）
   双保险：语音"确认" 或 烧录板 BOOT 物理确认（30s 超时作废，最多 3 轮）
3. 厨师（DSH）：建 boards/<id>.h → 编译 full.bin → 下发烧录指令（MQTT）
4. 烧录板：拉取 full.bin → 连接目标（等待 BOOT+RST）→ 流式烧录 → MD5 校验
   → 全程事件上报 fall/flasher/events（waiting/connected/progress/done/error）
5. 显示板/语音板 OLED：实时显示状态与进度条；出错时语音板 TTS 播报厨师的判断
6. 厨师监工：读事件流水 + 串口日志分析 → 异常时暂停、播报原因、等待用户反馈
   （用户反馈经服务员 dev_talk_to_dsh 传回厨师）
7. 烧完验证：目标板引导固件自检上报（boot:1/mac/fw）→ 遥测到达 → 登记注册表
→ 语音播报接入完成，可离线（OLED 显示板已退役）
8. 功能迭代：DSH 判断 无线（OTA）/ 接线（新外设）→ 继续语音驱动

五、开发者模式与权限门控
────────────────────────────────────────────────────────────────
- 生产模式：仅只读查询 + 业务工具（工具列表不含烧录/迭代类）
- 开发者模式：语音"启用开发者模式"（复读确认）或 烧录板长按 BOOT 硬件入口
  → fall-mcp 按模式过滤 tools/list（LLM 看不到=调不了）
- 10 分钟无操作自动退出；模式状态 OLED/语音提示
- 高危操作分级确认：查询直行 / 普通执行+播报 / 高危（烧录/覆盖/模式切换）意图复读+物理确认

六、防呆体系（成熟标志）
────────────────────────────────────────────────────────────────
1. 意图级复读确认（防嘴瓢/LLM 幻觉）
2. 目标板检测：连接后读目标 flash 头，非空警告"已有系统，确认覆盖？"
3. 失败恢复：自动重试 3 次 + 错误原因播报 + 用户引导
4. DSH 失联防线：烧录板 60s 无人监工自动暂停保持现场
5. 双分区回滚：OTA 失败自动回滚（已有）
6. 审计日志：高危操作全量留痕（用户/意图/确认方式/目标/结果/耗时）→ 经验库素材
7. （暂缓）MAC 核对防串板 / 断点续烧 / 错误码体系

七、关键技术决策（已定）
────────────────────────────────────────────────────────────────
| 决策 | 理由 |
|---|---|
| 烧录板独立（v2 基线稳定内核） | OLED/I2C 与烧录高压并发崩溃，物理隔离根治 |
| 显示板独立（MQTT 事件驱动） | 解耦、可放任意位置、语音板后续可兼显示 |
| v2 基线 = 无 OLED/按键/新 sdkconfig 的最小可靠内核 | 云端烧录 100% PASS 实证 |
| 事件流水 = 唯一事实源 | 双 LLM 记忆都从它读，避免状态漂移 |
| 语音板最终形态 = 语音 + OLED + 出错播报（不含烧录硬件） | 烧录归烧录板，语音归语音板 |
| DSH 监工 = 轮询事件 + LLM 分析串口日志 + 决策干预 | 厨师在线，服务员传话，顾客零障碍 |

八、路线图（实施顺序）
────────────────────────────────────────────────────────────────
已完成：阶段0-5、方向A、板烧板读写、云端烧录、烧录/显示分离、v11+显示固件、事件协议
🔨 当前：OLED 接线验证 → 端到端烧录演示
📌 #7  MQTT 指令执行器化（flash_start/abort/log 回传）——监工地基
📌 #8  DSH 监工循环（dev_flash_board 编排）
📌 #9  语音转述通道（dev_speak / dev_talk_to_dsh）
📌 #10 开发者模式权限门控 + 双入口
📌 #11 意图级复读确认（语音+BOOT 双保险）
📌 #15 引导固件自检上报（验证 90s→10s）
📌 #16 语音板集成（OLED+MQTT+出错 TTS，需用户确认断语音服务）
📌 #17 云端编排（first_flash 流程 + 接线/无线分流 + 经验入库）
📌 #18 防呆全量（重试/超时/失联防线/审计）
📌 #19 引导固件运行时配网（AP 模式，告别每板编译）——v2 远期
⏸️ 暂缓：#12 MAC 核对 / #13 断点续烧 / #14 错误码体系
⏸️ 界面中文化（用户要求英文优先）

九、最终形态一句话
────────────────────────────────────────────────────────────────
"顾客说话，服务员传话，厨师干活，双手执行，全程可视（OLED）、出错才开口（语音）、
一切可回溯（审计+经验）——一块陌生 ESP32 板，从白片到智能设备，全程语音指挥，一次接线。"

════════════════════════════════════════════════════════════════
